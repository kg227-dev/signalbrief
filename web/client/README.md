# Web Client

See [Repository Map](../../docs/repository-map.md) for browser-facing code placement.

Client pages and UI runtime scripts currently live at `web/*.html` and `web/*runtime.js` for static serving compatibility. New UI modules should be grouped under:

- `web/client/pages/`
- `web/client/ui-state/`
- `web/client/ui-actions/`
