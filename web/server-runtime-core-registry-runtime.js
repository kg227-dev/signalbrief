"use strict";

function createCoreRouteDependencies({ deps, sharedHandlers }) {
  const {
    json,
    DEFAULT_TOPICS,
    INDUSTRY_TOPICS,
    CAPABILITY_TOPICS,
    digestRunStatus,
    getCachedOrRefreshSchedulerHeartbeat,
    findUserByToken,
    signUnsubEmail,
    verifyUnsubEmailSignature,
    allUsers,
    writeUser,
    blankReengagementState,
    isLegacyArchiveEndpointEnabled,
    recordLegacyArchiveUsage,
    getArchiveLegacyDeprecationDeadlineUtc,
    readArchiveFilesForDir,
    getAllowedArchiveDatesForUser,
    archiveRelevanceScore,
    path,
    fs,
    APP_ROOT,
    decodeDigestIdParam,
    buildDigestId,
    toEtDateKey,
    appendWebEngagementEvent,
    resetReengagementState,
    sendTransparentGif,
    normalizeEngagementUrl,
    requireJsonBody,
    normalizeBookmarkUrl,
    sendMagicLinkEmail,
  } = deps;
  const { handleSignup, handleSettings } = sharedHandlers;

  return {
    json,
    DEFAULT_TOPICS,
    INDUSTRY_TOPICS,
    CAPABILITY_TOPICS,
    digestRunStatus,
    getCachedOrRefreshSchedulerHeartbeat,
    findUserByToken,
    handleSignup,
    handleSettings,
    signUnsubEmail,
    verifyUnsubEmailSignature,
    allUsers,
    writeUser,
    blankReengagementState,
    isLegacyArchiveEndpointEnabled,
    recordLegacyArchiveUsage,
    getArchiveLegacyDeprecationDeadlineUtc,
    readArchiveFiles: readArchiveFilesForDir,
    getAllowedArchiveDates: getAllowedArchiveDatesForUser,
    archiveRelevanceScore,
    path,
    fs,
    APP_ROOT,
    decodeDigestIdParam,
    buildDigestId,
    toEtDateKey,
    appendEngagementEventChecked: appendWebEngagementEvent,
    resetReengagementState,
    sendTransparentGif,
    normalizeEngagementUrl,
    requireJsonBody,
    normalizeBookmarkUrl,
    sendMagicLinkEmail,
  };
}

module.exports = {
  createCoreRouteDependencies,
};
